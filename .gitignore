# Sync state is per-installation: it holds a real board id, repo slug, and
# item mapping. It belongs in the *user's* project, never in the skill repo.
.monday-sync/

# Build output is regenerated by packaging/release.sh
dist/
scripts/__pycache__/
tests/__pycache__/
.pytest_cache/
